# This is the original non-federated GraphQL API
type Query {
    users: [User]
}

type User {
    id: ID!
    name: String
    address: Address
}

type Address {
    street: String
    city: String
    country: String
}
