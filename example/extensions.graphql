# This is not another subgraph schema, but a modification for how you want
# the non-federated graph to be updated. It is not valid GraphQL on its own.

type User @key(fields: "id")

type Address @shareable
